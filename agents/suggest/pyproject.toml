[project]
name = "suggest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "core"
]

[tool.uv.build-backend]
module-name = "suggest"